@extends('layouts.app')

@section('content')
<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-erp-text">Forecasting Analytics</h1>
    <p class="text-erp-textMuted mt-1">Detailed insights and recommendations</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    @foreach ($insights as $key => $value)
    <div class="bg-white p-6 rounded-xl shadow-sm border border-erp-border">
      <h3 class="text-lg font-bold text-erp-text capitalize">{{ str_replace('_', ' ', $key) }}</h3>
      <p class="text-erp-textMuted mt-2">{{ $value }}</p>
    </div>
    @endforeach
  </div>
</div>
@endsection

